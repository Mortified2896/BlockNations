{
  "name": "pbp-turn-relay",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "check": "npm run lint && node --check server.js && node --check server.prod.js",
    "lint": "eslint server.js server.prod.js",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.1"
  }
}
